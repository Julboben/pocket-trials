name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Syntax check
        run: npm run check
      - name: Level catalog is up to date
        run: |
          npm run levels
          git diff --exit-code levels/catalog.json
      - name: Type check
        run: npm run typecheck
      - name: Tests
        run: npm test
